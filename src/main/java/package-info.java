/**
 * A small library for outputting text-based tree structures, particularly in a terminal
 * environment. {@link TreeWriter} is the key class for client code to use.
 */
package au.djac.treewriter;
